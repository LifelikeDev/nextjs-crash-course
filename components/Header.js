import Head from "next/head";

const Header = () => {
  return (
    <Head>
      <title>NextJS Tutorial</title>
      <meta
        name="keywords"
        content="web development, client-side, server-side, frontend, react, nextjs"
      />
    </Head>
  );
};

export default Header;
